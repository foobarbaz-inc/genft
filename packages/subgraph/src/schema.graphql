type Job @entity {
  id: ID!
  modelCategory: Int! # uint8
  seed: Bytes!
  modelConfigLocation: String!
  inputDataLocationType: Int!
  input: Bytes!
  outputDataLocationType: Int!
  outputDataFormat: Int!
  createdTimestamp: BigInt!
}
